# 機密情報が含まれることがしばしばあるため、GitIgnore追加推奨
aws_region  = "ap-northeast-1"
profile = "LogMon-Admin" # AWSアカウント名を指定する(aws configure list-profilesで確認する)
project_name = "dev-glico-sunao-terratest-dashboad"
env = "dev"